# Template — do NOT put real keys here. Copy this file to `.env` and fill it in there.
# `.env` is gitignored, `.env.example` is committed.
# Get a key at https://openrouter.ai/keys — one key reaches all 10 models.
OPENROUTER_API_KEY=

# Optional attribution headers (used by OpenRouter's public leaderboard).
OPENROUTER_REFERER=
OPENROUTER_TITLE=Quant Interview Benchmark
